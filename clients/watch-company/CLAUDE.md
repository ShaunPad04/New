# Watch Company Concept — Project Instructions

This directory is a standalone client concept workspace inside the `New` repository.

## Scope
- Work only inside `clients/watch-company/` unless the user explicitly approves a repository-level change.
- Inherit the repository's existing quality, accessibility, performance, SEO, security, testing, and design standards where applicable.
- Reuse existing tooling and skills instead of recreating them.
- Do not modify or delete the existing root website or other client work.
- Keep this concept non-indexable until the user explicitly approves launch/indexing.

## Workflow
1. Read `PRODUCT.md` and `DESIGN.md` before design or implementation work.
2. Inspect existing repository tooling before adding dependencies.
3. Make the smallest justified change, then verify it.
4. Stop for approval before major architectural or visual-direction changes.
